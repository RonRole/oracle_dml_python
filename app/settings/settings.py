class Settings:
    def __init__(
        self,
        host_ip,
        host_port,
        sid,
        username,
        password
    ):
        self.host_ip = host_ip
        self.host_port = host_port
        self.sid = sid
        self.username = username
        self.password = password
